config:
  target: 'https://api.pageifyapp.com'
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - flow:
    - post:
        url: /
        json :
          query: '{posts{text type text_color user{username}}}'